import { colors } from '../variables';
import { rgba } from 'polished';

export default {
	title: 'light',
	pageBackground: colors.light,
	text: colors.dark,
	buttonBackground: colors.dark,
	hoverButtonBackground: rgba(colors.dark, 0.7),
	buttonText: colors.light,
};
