import { colors } from '../variables';
import { rgba } from 'polished';

export default {
	title: 'dark',
	pageBackground: colors.dark,
	text: colors.clear,
	buttonBackground: colors.clear,
	hoverButtonBackground: rgba(colors.clear, 0.7),
	buttonText: colors.dark,
};
