import { colors } from '../variables';
import { rgba } from 'polished';

export default {
	title: 'home',
	pageBackground: colors.clear,
	text: colors.primary,
	buttonBackground: colors.primary,
	hoverButtonBackground: rgba(colors.primary, 0.7),
	buttonText: colors.clear,
};
